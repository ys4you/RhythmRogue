// The RelicEffect enum has been removed.
//
// Relics now use a list of typed RelicEffectDef objects (see RelicEffectDef.cs and
// RelicEffects.cs) rather than a single effect enum plus a shared int/float value. This file
// is an empty leftover and can be deleted from the project (Project window -> right-click -> Delete).
